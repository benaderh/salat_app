<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Alarmes exactes -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Exemption optimisation batterie -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Mode silencieux / acces Ne pas deranger -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Lecture fichiers mp3 sur le stockage partage -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".SalatApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SalatTimes"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.SalatTimes.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsVilleActivity"
            android:exported="false"
            android:label="@string/select_city"
            android:theme="@style/Theme.SalatTimes" />

        <activity
            android:name=".SettingsAlarmesActivity"
            android:exported="false"
            android:label="@string/alarm_settings"
            android:theme="@style/Theme.SalatTimes" />

        <activity
            android:name=".SoundPickerActivity"
            android:exported="false"
            android:label="@string/choose_sound"
            android:theme="@style/Theme.SalatTimes" />

        <activity
            android:name=".BatteryPermissionActivity"
            android:exported="false"
            android:theme="@style/Theme.SalatTimes" />

        <!-- Recepteur des alarmes exactes (avant + a l'heure) -->
        <receiver
            android:name=".alarm.PrayerAlarmReceiver"
            android:exported="false" />

        <!-- Recepteur pour reprogrammer les alarmes apres redemarrage du telephone -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Recepteur de fin de periode silencieuse (retour au mode sonore precedent) -->
        <receiver
            android:name=".alarm.SilentModeEndReceiver"
            android:exported="false" />

        <!-- Service foreground qui joue le son d'alarme meme app fermee -->
        <service
            android:name=".alarm.AthanPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

    </application>

</manifest>
